defaults:
  - /base

actor_rollout_ref:
  rollout:
    response_length: 512
    max_model_len: 8096

agent_proxy:
  max_turn: 15
  max_actions_per_turn: 4

val_agent_proxy:
  max_turn: 15

es_manager:
  train:
    env_configs:
      tags: ["Lean"]
  val:
    env_configs:
      tags: ["Lean"]
